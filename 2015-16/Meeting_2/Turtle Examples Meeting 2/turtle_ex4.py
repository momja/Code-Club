import turtle
t = turtle.Turtle()
t.speed(0)
#this for loop will run 20 times
for i in range(20):
    #the variable i starts at zero
    #every time the loop runs, i increases by one
    t.forward(i * 10)
    t.right(144)
